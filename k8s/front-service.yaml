apiVersion: v1
kind: Service
metadata:
  name: front
  labels:
    app.kubernetes.io/name: quotes-app
    app.kubernetes.io/component: frontend
spec:
  selector:
    app: front
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30001
  type: NodePort